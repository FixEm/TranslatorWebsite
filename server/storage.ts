import 'dotenv/config';

// Simple test to see if the module loads
console.log('Storage module loading...');

// Export a simple object for now to test
export const storage = {
  test: true
};

export default storage;

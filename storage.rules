rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all files
    match /{allPaths=**} {
      allow read: if true;
      // Allow write access for now (you can restrict this later with authentication)
      allow write: if true;
    }
  }
}
